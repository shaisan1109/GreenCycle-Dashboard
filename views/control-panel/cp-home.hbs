<!-- Application counts by status -->
<div class="info-grid" style="margin-bottom: 1.5rem;">
  <div class="form-section stat-box text-center">
        <div class="stat-value">{{entryCount.approved}}</div>
        <span>Total Approved Entries</span>
  </div>

  <div class="form-section stat-box text-center">
        <div class="stat-value">{{entryCount.pending}}</div>
        <span>Total Pending Entries</span>
  </div>

  <div class="form-section stat-box text-center">
        <div class="stat-value">{{entryCount.rejected}}</div>
        <span>Total Rejected Entries</span>
  </div>

  <div class="form-section stat-box text-center top-region">
    <div class="stat-value">{{topRegions.[0].region_name}}</div>
    <span>Top Reporting Region</span>
  </div>
</div>

<!-- Latest Submissions + Top Contributors -->
<div class="info-grid">
  <!-- Latest Submissions -->
  <div class="form-section" style="flex: 2;">
    <h3><i class="fas fa-clock"></i> Latest Submissions</h3>
    {{#each latestSubmissions}}
      <div class="submission-card">
        <div>
          <span class="submission-title">{{title}}</span><br>
          {{uppercase lastname}}, {{firstname}}
        </div>
        <div class="submission-status">
          <span class="data-status 
            {{#ifEquals status 'Pending Review'}}stat-pending{{/ifEquals}} 
            {{#ifEquals status 'Approved'}}stat-approved{{/ifEquals}} 
            {{#ifEquals status 'Rejected'}}stat-rejected{{/ifEquals}}">
            {{status}}
          </span><br>
          <i>{{textDate date_submitted}}</i>
        </div>
      </div>
    {{/each}}
    <button class="btn-submit">See all submissions</button>
  </div>

  <!-- Top Contributors + Regions -->
  <div class="form-section" style="flex: 1;">
    <h3><i class="fas fa-users"></i> Top Contributors</h3>
    {{#each contributors}}
      <div class="ranked-item">
        {{uppercase lastname}}, {{firstname}}
        <span class="ranked-count">{{entry_count}}</span>
      </div>
    {{/each}}
    <button class="btn-submit">See all top contributors</button>

    <hr style="margin: 1rem 0;">

    <h3><i class="fas fa-map-marker-alt"></i> Top Reporting Regions</h3>
    {{#each topRegions}}
      <div class="ranked-item">
        {{region_name}}
        <span class="ranked-count">{{entry_count}}</span>
      </div>
    {{/each}}
    <button class="btn-submit">See all top reporting regions</button>
  </div>
</div>

<!-- Submission History (Chart) -->
<div class="form-section">
  <h3><i class="fas fa-chart-line"></i> Submission History</h3>
  <canvas id="monthlySubmissionsChart" width="600" height="300"></canvas>
</div>

<!-- Script for monthly submissions -->
<script>

const monthlySubmissions = {{{monthlySubmissions}}}

const ctx = document.getElementById('monthlySubmissionsChart').getContext('2d');
  new Chart(ctx, {
  type: 'line',
  data: monthlySubmissions,
  options: {
    responsive: true,
    plugins: {
      title: {
        display: true,
        text: 'Monthly Data Submissions'
      },
      legend: {
        display: false
      }
    },
    scales: {
      x: {
        title: {
          display: true,
          text: 'Month'
        }
      },
      y: {
        beginAtZero: true,
        title: {
          display: true,
          text: 'Entries'
        }
      }
    }
  }
});

</script>

<style>

/* Shared layout utility */
.text-center {
  text-align: center;
}

/* Stats summary box */
.stat-box {
  flex: 1 1 200px;
  padding: 1rem !important;
  flex-direction: column;
}

.stat-value {
  font-size: 2rem;
  font-weight: bold;
  color: #127009;
}

.top-region {
    flex: 2 1 400px; /* grows twice as wide as others */
}

/* Submission entries list */
.submission-card {
  display: flex;
  justify-content: space-between;
  padding: 0.8rem 0;
  border-bottom: 1px solid #eee;
}

.submission-title {
  font-size: 1.1rem;
  font-weight: bold;
}

.submission-status {
  text-align: right;
  font-size: 0.9rem;
}

/* Ranked list (contributors / regions) */
.ranked-item {
  display: flex;
  justify-content: space-between;
  padding: 0.4rem 0;
  border-bottom: 1px solid #eee;
}

.ranked-count {
  font-weight: bold;
  color: #127009;
}

/* Chart wrapper */
#monthlySubmissionsChart {
  max-width: 100%;
}

.form-section {
  background-color: #ffffff;
  padding: 1.5rem;
  margin-bottom: 1rem;
  border: 1px solid #ddd;
  border-radius: 12px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.05);
}

.btn-submit {
  background-color: #127009;
  color: white;
  padding: 0.6rem 1.2rem;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  cursor: pointer;
  transition: background-color 0.2s ease;
}

.btn-submit:hover {
  background-color: #0e5b07;
}

.info-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 1.5rem;
}

</style>